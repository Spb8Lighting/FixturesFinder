language: 
  - node_js
node_js:
  - "node"

cache:
  directories:
    - node_modules

notifications:
  email:
    - false

sudo:
  - required

install:
  - npm install --unsafe-perm=true --allow-root
  - npm run rebuild

script:
  - gulp js
  - npm run build

os: 
  - linux

dist:
  - trusty

