language: 
  - node
node_js:
  - "iojs"

cache:
  directories:
    - node_modules

notifications:
  email:
    - false

sudo:
  - required

install:
  - sudo npm install --unsafe-perm=true --allow-root
  - sudo npm run rebuild

script:
  - npm run start
  - gulp

os: 
  - linux

dist:
  - trusty

